# CLAUDE.md — Logic Architecture

## Проект

Веб-сайт и маркетинговые материалы компании **Logic Architecture** — консалтинговой и девелоперской компании с собственным AI-стеком и опытом в банковском секторе. Маркетинговые исследования, разработка продуктов, техническая документация, помощь студентам с подготовкой учебных работ.

## Структура проекта

```
C:\Logic-Architecture\                 # ← корень проекта (git root)
├── CLAUDE.md                          # Этот файл
├── README.md                          # README проекта
├── docker-compose.yml                 # Docker Compose (nginx + api)
│
├── docker\                            # Docker-конфигурация для VPS
│   ├── nginx\
│   │   ├── Dockerfile                 # nginx:1.25.5-alpine
│   │   ├── nginx.conf                 # Основной конфиг nginx
│   │   └── default.conf               # Серверный блок (SSL, proxy)
│   └── api\
│       └── Dockerfile                 # Node.js API
│
├── server\                            # Express API для VPS
│   ├── server.js                      # Express-сервер (POST /api/order, /api/join)
│   ├── package.json                   # Зависимости (express, node-fetch)
│   └── .env.example                   # Шаблон переменных окружения
│
├── docs\                              # Сайт и контент
│   ├── index.html                     # Сайт Logic Architecture (single-file SPA)
│   ├── logo.jpg                       # Логотип (LA с элементами микросхем)
│   ├── founder.jpg                    # Фото основателя
│   ├── _headers                       # Security headers для Cloudflare Pages (legacy)
│   │
│   ├── i18n\                          # Переводы интерфейса
│   │   ├── en.json                    # Английский
│   │   ├── vi.json                    # Вьетнамский
│   │   └── zh.json                    # Китайский
│   │
│   ├── strategy\                      # Стратегия и аналитика
│   │   └── marketing_strategy.md      # Маркетинговая стратегия v3.0 (консалтинг + разработка + образование)
│   │
│   └── plans\                         # Планы реализации (создаются Claude)
│       └── expressive-beaming-otter.md    # Текущий план: актуализация документации
│
├── worker\                            # Cloudflare Worker (бэкенд форм)
│   ├── worker.js                      # Worker код (POST /order, /join → Telegram)
│   ├── wrangler.toml                  # Конфигурация деплоя Worker
│   └── .dev.vars                      # Локальные секреты (в .gitignore)
│
└── examples\                          # Примеры выполненных работ (портфолио)
    ├── ПРИМЕР_КУРСОВАЯ_ЗАЩИТА_СОБСТВЕННОСТИ.md
    ├── КУРСОВАЯ_ПЛАГИАТ_МУЗЫКА.md
    ├── Статья_объективизация_доказывания.md
    └── Статья_объективизация_доказывания.docx
```

## Технический стек сайта

### Архитектура
- **Single-file SPA** — весь HTML, CSS, JS в одном файле `index.html`
- **Табовая навигация** — `switchTab()` переключает `data-tab` ↔ `id="tab-{name}"`
- **Accordion** — в Услугах: `toggleAccordion()` с анимацией max-height
- **Canvas-анимация** — тематические иконки с хаотичным движением, молниеобразные связи, tendrils
- **i18n** — 4 языка (ru по умолчанию, en, vi, zh), загрузка JSON, `data-i18n` / `data-i18n-html` / `data-i18n-ph`
- **Typed.js** — анимация набора текста в hero (CDN: unpkg.com)
- **AOS** — scroll-анимации (CDN: unpkg.com)
- **Без фреймворков** — чистый HTML/CSS/JS, Google Fonts (Inter + Space Grotesk)

### Вкладки (tabs)
| data-tab | id | Описание |
|---|---|---|
| `home` | `tab-home` | Главная: hero «Проектируем и строим» + 6 карточек ценностей + metrics |
| `services` | `tab-services` | Услуги: 8 accordion-карточек (7 услуг + ссылка на Студентам) |
| `students` | `tab-students` | Студентам: боли + 6 пакетов услуг с ценами + inclusions |
| `tech` | `tab-tech` | Разработчикам (tech-grid + stack-bar + arch) |
| `investors` | `tab-investors` | Инвесторам (inv-grid) |
| `about` | `tab-about` | О нас: фото основателя + timeline + Global Community блок |
| `join` | `tab-join` | Присоединиться (форма → Worker → Telegram) |

### Услуги (accordion, tab-services)
8 позиций `.svc-item` с `.svc-head` + `.svc-body`:
1. 🔍 Research — Маркетинговые исследования
2. 📊 Strategy — Маркетинговая стратегия
3. 📢 Advertising — Реклама
4. 🌐 Web Development — Разработка сайтов и веб-сервисов
5. 📱 Mobile Development — Разработка мобильных приложений
6. 📋 Project Docs — Проектная документация
7. 📄 Technical Docs — Техническая документация
8. 🎓 Education — Услуги для студентов (ссылка на tab-students)

### Студентам (tab-students)
- **Боли** — 6 карточек `.feat` (антиплагиат, возвраты, нет времени и т.д.)
- **Пакеты** — 6 карточек `.pkg` с ценами:
  - Рефераты от 3к, курсовые от 12к (`.hot`), дипломы от 35к
  - Магистерские от 50к, статьи от 15к, оригинальность от 5к
- **Включено** — 7 пунктов `.incl` (консультация, источники, оформление, антиплагиат и т.д.)

### CSS-переменные
```css
--bg:#07070d  --bg2:#0d0d16  --card:rgba(255,255,255,0.03)  --card-h:rgba(255,255,255,0.06)
--blue:#4f7df5  --purple:#6c5ce7  --gold:#d4a853  --gold-l:#f0c866
--cyan:#22d3ee  --green:#4ade80  --red:#f87171
--t1:#ffffff  --t2:#8a8a9a  --t3:#5a5a6a
--brd:rgba(255,255,255,0.06)  --brd-a:rgba(79,125,245,0.4)
--glow-b:rgba(79,125,245,0.15)  --glow-g:rgba(212,168,83,0.2)
--glass:rgba(13,13,22,0.88)  --glass-b:rgba(255,255,255,0.08)
```

### CSS-паттерны (именование)
При добавлении новых страниц-табов используй существующие паттерны:
- **Hero-блок:** `.{prefix}-hero` (biz-hero, inv-hero, join-hero)
- **Сетка карточек:** `.{prefix}-grid` (biz-grid, inv-grid, tech-grid, pkg-grid)
- **Карточка:** `.{prefix}-card` (biz-card, inv-card, tcard, `.pkg`, `.feat`)
- **CTA-блок:** `.{prefix}-cta` (biz-cta, inv-cta)
- **Accordion:** `.svc-list` → `.svc-item` → `.svc-head` / `.svc-body` → `.svc-body-inner`
- **Section header:** `.sh` → `.label` + `h2` + `p`
- **Reveal-анимация:** `data-aos="fade-up"` с `data-aos-delay`
- **Кнопки:** `.btn--gold`, `.btn--blue`, `.btn--green`, `.btn--outline`, `.btn--tg`

### Responsive breakpoints
| Брейкпоинт | Что меняется |
|---|---|
| `1100px` | Сетки → 2 колонки, навигация уменьшается |
| `1024px` | Сетки → 1 колонка, flex-блоки → column |
| `900px` | Мобильное меню (гамбургер `.ham`), скрытие навигации |
| `768px` | Компактные grid-карточки (36px 1fr), минимальные отступы |
| `480px` | Ультра-компакт: логотип → "LA" (`.logo-text-short`), минимальные размеры |

### Мобильный паттерн компактных карточек (≤768px)
На мобильном все карточки используют единый паттерн:
```css
display:grid; grid-template-columns:36px 1fr; gap:0 12px; padding:14px 16px;
```
Иконка 36×36 слева, заголовок + текст справа. Применено к:
- `.feat` (блоки «болей» на Студентам)
- `.tcard` (карточки технологий)
- `.pkg` (карточки услуг — иконка 40px + бейдж)
- `.join-role` (роли в «Присоединиться»)

### Модальное окно контакта
- Открытие: `openModal()`, закрытие: `closeModal(event)`, Escape
- Содержимое: текст-инструкция + список + кнопка «Написать в Telegram» → `t.me/LogicArchitecture`
- Класс `.modal--contact`, кнопка `.btn--tg` (градиент Telegram #2AABEE → #229ED9)
- Все CTA-кнопки на сайте ведут на `openModal()` без аргументов
- Альтернативная ссылка `.modal-alt` на Telegram

### Toast-уведомления
- `showToast(msg, type)` — временное уведомление (используется при отправке формы)
- Класс `.toast` → `.show` для отображения

## Правовые ограничения (КРИТИЧЕСКИ ВАЖНО)

### ФЗ-383 от 30.10.2018
**Полный запрет на рекламу** услуг по написанию учебных работ.

#### Запрещённые формулировки (B2C)
❌ «написание на заказ», «написать диплом», «заказать работу», «купить», «сделаем за вас», «закажите»

#### Безопасные формулировки (B2C)
✅ «помощь с подготовкой», «помощь в написании», «сопровождение и консультирование», «получить консультацию», «помощь студентам», «поможем подготовить», «узнайте стоимость»

#### B2B — без ограничений
Техническая документация для бизнеса **НЕ подпадает** под ФЗ-383. Можно использовать прямой язык, скидки, акции, любые рекламные каналы.

### ERID-маркировка
С сентября 2022 вся интернет-реклама в РФ требует пометку «Реклама» и ERID. Штраф до 500 000 ₽.

### Дисклеймер
> «Материалы, предоставляемые Logic Architecture в рамках образовательных услуг, носят вспомогательный характер и предназначены для помощи в подготовке учебных работ.»

## Маркетинговая стратегия

Файл: `docs/strategy/marketing_strategy.md` (v3.0)

### Целевые сегменты
| Сегмент | Ср. чек | Приоритет |
|---|---|---|
| Стартапы и малый бизнес | 30-100к | Основной (маркетинг, разработка) |
| Средний бизнес (B2B) | 50-200к | Растущий (документация, исследования) |
| Студенты (B2C) | 3-50к | Образовательные услуги |

### Ценообразование (синхронизировано с сайтом)
**B2C (tab-students):** Рефераты от 3к, курсовые от 12к, дипломы от 35к, магистерские от 50к, статьи от 15к, оригинальность от 5к
**B2B (accordion в tab-services):** Исследования от 30к, стратегия от 40к, реклама от 25к, веб-разработка от 50к, мобильная разработка от 100к, проектная документация от 25к, техническая документация от 20к

### Каналы и контакты
- **Telegram** — основной канал связи с клиентами: `@LogicArchitecture` (t.me/LogicArchitecture)
- **Telegram-бот** — получает заявки с формы «Присоединиться» через Worker → Bot API
- **Авито** — листинг B2C и B2B
- **SEO** — органический трафик
- **Habr** — B2B экспертные статьи (с месяца 4)
- **Сарафанное радио** — реферальная программа

## Инфраструктура и деплой

### Хостинг (продакшн)
- **VPS + Docker** — nginx + Express API
- **nginx 1.25.5** — раздаёт статику из `docs/`, SSL через certbot (Let's Encrypt)
- **Docker Compose** — `docker-compose.yml` (nginx + api сервисы)
- **Домен:** `https://logic-architecture.com` (основной)
- **HSTS:** `Strict-Transport-Security: max-age=63072000; includeSubDomains`

### Cloudflare Pages (вторичный)
- **URL:** `https://logic-architecture.pages.dev`
- Publish directory: `docs/`
- **Автодеплой** при `git push origin main`

### Cloudflare Worker (бэкенд форм)
- **URL:** `https://la-api.ravinski-genlawyer.workers.dev`
- **Эндпоинты:** `POST /order` (заявка — не используется с сайта, модалка ведёт в Telegram), `POST /join` (присоединение — активно)
- **Конфиг:** `worker/wrangler.toml`
- **Код:** `worker/worker.js`
- **Secrets:** `TG_TOKEN`, `TG_CHAT_ID` (через `wrangler secret put`)
- Форма «Присоединиться» → Worker → Telegram Bot API → чат основателя
- CORS: `ALLOWED_ORIGIN` в env (должен включать `https://logic-architecture.com`)

### Express API (VPS, дублирует Worker)
- **Код:** `server/server.js`
- **Эндпоинты:** `POST /api/order`, `POST /api/join`
- **Конфиг:** `server/.env` (TG_TOKEN, TG_CHAT_ID, ALLOWED_ORIGIN, PORT)

### Антиспам
- Honeypot-поле (`data.website`) — бот заполняет, обычный пользователь нет
- Rate limit через `localStorage` — 1 заявка в 60 секунд (`la_rl`)
- Задержка 3 секунды после загрузки страницы

### Инструменты Claude (MCP)
Claude имеет доступ к MCP-серверам для управления компьютером и браузером:

- **Windows MCP** — управление Windows-приложениями:
  - Запуск приложений, клики по координатам, ввод текста, скриншоты
  - Команды: `Snapshot`, `Click`, `Type`, `Shell`, `App`, `Shortcut`
  - Используется для тестирования сайта и управления приложениями
- **Деплой Worker:** `wrangler deploy` (bash) из директории `worker/`
- **Cloudflare API:** Global API Key, аутентификация через `X-Auth-Email` + `X-Auth-Key`
  - Account ID: `66893112619726c1e0e7a5d1d9149dac`
  - Email: `ravinski.genlawyer@gmail.com`

## Конвенции разработки

### При редактировании index.html
1. **Не разбивать на файлы** — проект осознанно сделан single-file
2. **Минифицировать CSS** — стили написаны компактно, без лишних пробелов
3. **Сохранять порядок секций** — CSS комментарии `/* ===== SECTION ===== */` разделяют блоки
4. **Добавлять `data-aos="fade-up"`** к блокам, которые должны анимироваться при скролле
5. **iOS Safari** — `inset` всегда дублировать через `top/right/bottom/left`, `-webkit-backdrop-filter` параллельно с `backdrop-filter`
6. **Responsive** — при добавлении новых классов добавлять правила во все нужные брейкпоинты
7. **i18n** — добавлять `data-i18n` ключи к новым элементам, обновлять en.json/vi.json/zh.json

### При редактировании стратегии
1. Все формулировки проверять по справочнику безопасных фраз (раздел 1.2 стратегии)
2. Цены должны быть синхронизированы между `docs/index.html` и `docs/strategy/marketing_strategy.md`
3. B2B-материалы маркировать отдельно, указывая что ФЗ-383 не применяется

### Работа с файлами на Windows
- Использовать прямые слэши (`C:/Logic-Architecture/docs/`) в bash-командах
- Пути с кириллицей и пробелами оборачивать в двойные кавычки
- Не использовать backslash (`\`) в mv/cp/rm командах — приводит к созданию мусорных файлов
- **Рабочий стол** пользователя: `C:/Users/ravin/OneDrive/Рабочий стол/` (НЕ `C:/Users/ravin/Desktop/`)
- Установлен **pandoc** для конвертации документов (md → docx и др.)

### Общие правила
- Язык интерфейса: **русский**
- Тон: **экспертный, уверенный, поддерживающий** (не агрессивный, не панибратский)
- Никогда не использовать «DAO» в клиентском позиционировании
- Личный бренд основателя: «Ex-Head of Systems Analysis, крупнейшие банки РФ»
- Не использовать: «генерация текстов», «написание нейросетью», «AI-инжиниринг текстов»
- Использовать: «разработал методику подготовки», «технологический подход»
